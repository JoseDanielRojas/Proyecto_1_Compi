!  lets anidados, con error
let

  var a := 1 ! debe inferir que a : Integer

in

  let
    const b ~ a ; ! es visible aquí
    var   b := a + 1 ! debe inferir que nueva a : Integer, ERROR: no se puede asignar a una constante
  in
    putint (a) ! debe imprimir 2 (no debería generar código
  end

end
 


